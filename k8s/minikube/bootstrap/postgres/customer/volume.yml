apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgres-customer-pv
  labels:
    type: local
    app: postgres-customer
spec:
  storageClassName: manual
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /mnt/data/postgres-customer
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-customer-pvc
  labels:
    app: postgres-customer
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
